{
  "name": "voice-order-system",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": "22.x"
  },
  "scripts": {
    "test": "node --test",
    "check": "node --check converse.js && node --check api/converse.js"
  }
}
